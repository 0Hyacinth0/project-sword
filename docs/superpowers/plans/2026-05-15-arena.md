# 竞技场入口与赛季展示 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现竞技场面板，展示赛季信息、6级段位系统、战绩统计，作为底部导航新 Tab 嵌入 HomeView。

**Architecture:** 新建类型、配置、API、Store、Panel 五层文件，遵循项目现有 Pinia Composition API + Mock API 模式。底部导航新增"竞技"Tab，centerView 扩展 `arena` 值。

**Tech Stack:** Vue 3 + TypeScript + Pinia + Liquid Glass CSS

---

## File Structure

| File | Action | Responsibility |
|:---|:---|:---|
| `src/types/arena.ts` | Create | 竞技场类型定义 |
| `src/config/arena_config.ts` | Create | 段位配置（6级 + 小级 + 颜色 + 积分范围） |
| `src/api/arena.ts` | Create | API 层 + Mock 数据 |
| `src/stores/arena.ts` | Create | Pinia 状态管理 |
| `src/components/arena/ArenaPanel.vue` | Create | 竞技场面板组件 |
| `src/views/HomeView.vue` | Modify | 底部导航 + centerView |

---

### Task 1: 定义竞技场类型

**Files:**
- Create: `src/types/arena.ts`

- [ ] **Step 1: 创建类型文件**

```typescript
/**
 * 竞技场系统类型定义
 * 赛季、段位、战绩等核心类型
 */

/** 段位等级 */
export type ArenaTier = 'bronze' | 'silver' | 'gold' | 'platinum' | 'diamond' | 'king'

/** 段位小级 */
export type ArenaSubTier = 'I' | 'II' | 'III'

/** 赛季信息 */
export interface ArenaSeason {
  /** 赛季 ID */
  seasonId: string
  /** 赛季名称 */
  seasonName: string
  /** 赛季编号 */
  seasonNumber: number
  /** 开始日期 */
  startDate: string
  /** 结束日期 */
  endDate: string
  /** 是否进行中 */
  isActive: boolean
}

/** 竞技场玩家数据 */
export interface ArenaPlayerData {
  /** 当前段位 */
  tier: ArenaTier
  /** 当前小级 */
  subTier: ArenaSubTier
  /** 积分 */
  score: number
  /** 胜场 */
  wins: number
  /** 败场 */
  losses: number
  /** 胜率 */
  winRate: number
  /** 赛季 ID */
  seasonId: string
}

/** 段位配置 */
export interface ArenaTierConfig {
  /** 段位标识 */
  tier: ArenaTier
  /** 段位中文名 */
  name: string
  /** 最低积分 */
  minScore: number
  /** 最高积分（-1 表示无上限） */
  maxScore: number
  /** 颜色 */
  color: string
  /** 图标 */
  icon: string
}

/** 段位解析结果 */
export interface TierInfo {
  tier: ArenaTier
  subTier: ArenaSubTier
  tierName: string
  progress: number
  remainingScore: number
}
```

- [ ] **Step 2: Commit**

```bash
git add src/types/arena.ts
git commit -m "feat: add arena type definitions"
```

---

### Task 2: 创建段位配置

**Files:**
- Create: `src/config/arena_config.ts`

- [ ] **Step 1: 创建配置文件**

```typescript
/**
 * 竞技场段位配置
 * 6级段位：青铜/白银/黄金/铂金/钻石/王者
 * 每级分 I/II/III 三个小级（王者除外）
 */
import type { ArenaTierConfig, ArenaTier, ArenaSubTier, TierInfo } from '../types/arena'

/** 段位配置列表（按积分升序） */
export const ARENA_TIER_CONFIGS: ArenaTierConfig[] = [
  { tier: 'bronze', name: '青铜', minScore: 0, maxScore: 599, color: '#8b8b8b', icon: '◆' },
  { tier: 'silver', name: '白银', minScore: 600, maxScore: 1199, color: '#c0c0c0', icon: '◆' },
  { tier: 'gold', name: '黄金', minScore: 1200, maxScore: 1799, color: '#ffd700', icon: '◆' },
  { tier: 'platinum', name: '铂金', minScore: 1800, maxScore: 2399, color: '#00b894', icon: '◆' },
  { tier: 'diamond', name: '钻石', minScore: 2400, maxScore: 2799, color: '#7c5cfc', icon: '◆' },
  { tier: 'king', name: '王者', minScore: 2800, maxScore: -1, color: '#ff6b6b', icon: '👑' }
]

/** 小级列表（从低到高：III → II → I） */
const SUB_TIERS: ArenaSubTier[] = ['III', 'II', 'I']

/**
 * 根据积分解析段位和小级
 * @param score - 竞技积分
 * @returns 段位信息
 */
export function resolveTier(score: number): TierInfo {
  let matchedConfig = ARENA_TIER_CONFIGS[0]

  for (const config of ARENA_TIER_CONFIGS) {
    if (config.maxScore === -1 || score <= config.maxScore) {
      matchedConfig = config
      break
    }
  }

  // 王者无小级
  if (matchedConfig.tier === 'king') {
    return {
      tier: 'king',
      subTier: 'I',
      tierName: `${matchedConfig.icon} ${matchedConfig.name}`,
      progress: 1,
      remainingScore: 0
    }
  }

  // 计算小级
  const range = matchedConfig.maxScore - matchedConfig.minScore + 1
  const subRange = Math.floor(range / 3)
  const offset = score - matchedConfig.minScore
  const subIndex = Math.min(2, Math.floor(offset / subRange))
  const subTier = SUB_TIERS[subIndex]

  // 计算当前小级进度
  const subFloor = matchedConfig.minScore + subIndex * subRange
  const subCeil = subIndex === 2 ? matchedConfig.maxScore + 1 : subFloor + subRange
  const progress = (score - subFloor) / (subCeil - subFloor)

  // 距下一个小级所需积分
  const remainingScore = subCeil - score

  return {
    tier: matchedConfig.tier,
    subTier,
    tierName: `${matchedConfig.icon} ${matchedConfig.name} ${subTier}`,
    progress,
    remainingScore
  }
}

/**
 * 获取段位配置
 * @param tier - 段位标识
 */
export function getTierConfig(tier: ArenaTier): ArenaTierConfig {
  return ARENA_TIER_CONFIGS.find(c => c.tier === tier) ?? ARENA_TIER_CONFIGS[0]
}
```

- [ ] **Step 2: Commit**

```bash
git add src/config/arena_config.ts
git commit -m "feat: add arena tier configuration and resolver"
```

---

### Task 3: 创建竞技场 API + Mock 数据

**Files:**
- Create: `src/api/arena.ts`

- [ ] **Step 1: 创建 API 文件**

```typescript
/**
 * 竞技场相关 API
 * 获取赛季信息、玩家竞技数据
 */
import request from './request'
import type { ApiResponse } from './request'
import type { ArenaSeason, ArenaPlayerData } from '../types/arena'
import { isMockEnabled } from '../utils/mockConfig'

/** Mock 延迟 */
function delay(ms: number): Promise<void> {
  return new Promise(resolve => setTimeout(resolve, ms))
}

/** Mock 赛季数据 */
const MOCK_SEASON: ArenaSeason = {
  seasonId: 'season-3',
  seasonName: '龙焰纪元',
  seasonNumber: 3,
  startDate: '2026-04-15T00:00:00Z',
  endDate: '2026-06-15T00:00:00Z',
  isActive: true
}

/** Mock 玩家数据 */
const MOCK_PLAYER: ArenaPlayerData = {
  tier: 'platinum',
  subTier: 'II',
  score: 2050,
  wins: 42,
  losses: 28,
  winRate: 60,
  seasonId: 'season-3'
}

/**
 * 获取当前赛季信息
 */
export async function getArenaSeasonApi(): Promise<ApiResponse<ArenaSeason>> {
  if (isMockEnabled()) {
    await delay(300)
    return { code: 200, message: '操作成功', data: MOCK_SEASON }
  }
  return request.get('/arena/season')
}

/**
 * 获取当前角色竞技数据
 */
export async function getArenaPlayerDataApi(): Promise<ApiResponse<ArenaPlayerData>> {
  if (isMockEnabled()) {
    await delay(300)
    return { code: 200, message: '操作成功', data: MOCK_PLAYER }
  }
  return request.get('/arena/me')
}
```

- [ ] **Step 2: Commit**

```bash
git add src/api/arena.ts
git commit -m "feat: add arena API with mock data"
```

---

### Task 4: 创建竞技场 Pinia Store

**Files:**
- Create: `src/stores/arena.ts`

- [ ] **Step 1: 创建 Store 文件**

```typescript
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import { getArenaSeasonApi, getArenaPlayerDataApi } from '../api/arena'
import { resolveTier, getTierConfig } from '../config/arena_config'
import type { ArenaSeason, ArenaPlayerData, TierInfo } from '../types/arena'

/**
 * 竞技场状态管理
 * 管理赛季信息和玩家竞技数据
 */
export const useArenaStore = defineStore('arena', () => {
  // ── 状态 ──
  const season = ref<ArenaSeason | null>(null)
  const playerData = ref<ArenaPlayerData | null>(null)
  const loading = ref(false)

  // ── 计算属性 ──

  /** 段位解析结果 */
  const tierInfo = computed<TierInfo | null>(() => {
    if (!playerData.value) return null
    return resolveTier(playerData.value.score)
  })

  /** 当前段位配置 */
  const currentTierConfig = computed(() => {
    if (!playerData.value) return null
    return getTierConfig(playerData.value.tier)
  })

  /** 赛季剩余天数 */
  const daysRemaining = computed(() => {
    if (!season.value || !season.value.isActive) return 0
    const end = new Date(season.value.endDate).getTime()
    const now = Date.now()
    return Math.max(0, Math.ceil((end - now) / (1000 * 60 * 60 * 24)))
  })

  /** 赛季剩余小时（不足1天的部分） */
  const hoursRemaining = computed(() => {
    if (!season.value || !season.value.isActive) return 0
    const end = new Date(season.value.endDate).getTime()
    const now = Date.now()
    const remaining = Math.max(0, end - now)
    return Math.floor((remaining % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60))
  })

  /** 赛季显示名称 */
  const seasonDisplayName = computed(() => {
    if (!season.value) return ''
    return `S${season.value.seasonNumber} · ${season.value.seasonName}`
  })

  /**
   * 获取赛季和玩家数据
   */
  async function fetchArenaData(): Promise<void> {
    loading.value = true
    try {
      const [seasonRes, playerRes] = await Promise.all([
        getArenaSeasonApi(),
        getArenaPlayerDataApi()
      ])
      if (seasonRes.code === 200) {
        season.value = seasonRes.data
      }
      if (playerRes.code === 200) {
        playerData.value = playerRes.data
      }
    } finally {
      loading.value = false
    }
  }

  return {
    season,
    playerData,
    loading,
    tierInfo,
    currentTierConfig,
    daysRemaining,
    hoursRemaining,
    seasonDisplayName,
    fetchArenaData
  }
})
```

- [ ] **Step 2: Commit**

```bash
git add src/stores/arena.ts
git commit -m "feat: add arena Pinia store"
```

---

### Task 5: 创建竞技场面板组件

**Files:**
- Create: `src/components/arena/ArenaPanel.vue`

- [ ] **Step 1: 创建组件文件**

```vue
<template>
  <div class="arena-panel">
    <!-- 加载态 -->
    <div v-if="store.loading" class="arena-panel__loading">
      <span class="loading-spinner"></span>
      <span>加载中...</span>
    </div>

    <template v-else-if="store.season && store.playerData && store.tierInfo">
      <!-- 赛季标题 -->
      <div class="arena-panel__season">
        <span class="arena-panel__season-label">当前赛季</span>
        <h2 class="arena-panel__season-name">{{ store.seasonDisplayName }}</h2>
        <span class="arena-panel__season-countdown">
          剩余 {{ store.daysRemaining }} 天 {{ store.hoursRemaining }} 小时
        </span>
      </div>

      <!-- 段位卡片 -->
      <div
        class="arena-panel__tier-card"
        :style="tierCardStyle"
      >
        <span class="tier-card__icon">{{ store.tierInfo.tierName.split(' ')[0] }}</span>
        <span class="tier-card__name">{{ store.currentTierConfig?.name }} {{ store.playerData.subTier }}</span>
        <span class="tier-card__score">积分: <strong>{{ store.playerData.score }}</strong></span>
        <div class="tier-card__progress">
          <div class="tier-card__progress-bar" :style="{ width: `${store.tierInfo.progress * 100}%` }"></div>
        </div>
        <span class="tier-card__remaining" v-if="store.tierInfo.remainingScore > 0">
          距{{ store.currentTierConfig?.name }} {{ nextSubTier }} 还需 {{ store.tierInfo.remainingScore }} 积分
        </span>
        <span class="tier-card__remaining tier-card__remaining--max" v-else>
          已达到当前段位上限
        </span>
      </div>

      <!-- 战绩统计 -->
      <div class="arena-panel__stats">
        <div class="stat-item">
          <span class="stat-item__value stat-item__value--win">{{ store.playerData.wins }}</span>
          <span class="stat-item__label">胜场</span>
        </div>
        <div class="stat-item">
          <span class="stat-item__value stat-item__value--lose">{{ store.playerData.losses }}</span>
          <span class="stat-item__label">败场</span>
        </div>
        <div class="stat-item">
          <span class="stat-item__value stat-item__value--rate">{{ store.playerData.winRate }}%</span>
          <span class="stat-item__label">胜率</span>
        </div>
      </div>

      <!-- 段位列表 -->
      <div class="arena-panel__tier-list">
        <div
          v-for="tier in tierList"
          :key="tier.tier"
          :class="['tier-row', { 'tier-row--current': tier.tier === store.playerData.tier }]"
          :style="tier.tier === store.playerData.tier ? { background: `${tier.color}14`, borderColor: `${tier.color}33` } : {}"
        >
          <span class="tier-row__icon" :style="{ color: tier.color }">{{ tier.icon }}</span>
          <span class="tier-row__name" :style="{ color: tier.color }">{{ tier.name }}</span>
          <span class="tier-row__range">{{ tier.maxScore === -1 ? `${tier.minScore}+` : `${tier.minScore}-${tier.maxScore}` }}</span>
          <span v-if="tier.tier === store.playerData.tier" class="tier-row__badge">当前</span>
        </div>
      </div>
    </template>

    <!-- 无数据 -->
    <div v-else class="arena-panel__empty">
      <span>暂无竞技数据</span>
    </div>
  </div>
</template>

<script setup lang="ts">
import { computed, onMounted } from 'vue'
import { useArenaStore } from '../../stores/arena'
import { ARENA_TIER_CONFIGS } from '../../config/arena_config'
import type { ArenaSubTier } from '../../types/arena'

const store = useArenaStore()

/** 段位列表（从高到低排列） */
const tierList = computed(() => [...ARENA_TIER_CONFIGS].reverse())

/** 段位卡片渐变背景 */
const tierCardStyle = computed(() => {
  const color = store.currentTierConfig?.color ?? '#00b894'
  return {
    background: `linear-gradient(135deg, ${color}1f, ${color}0a)`,
    borderColor: `${color}33`
  }
})

/** 下一个小级 */
const nextSubTier = computed((): string => {
  const sub = store.playerData?.subTier
  const map: Record<string, string> = { 'III': 'II', 'II': 'I' }
  return map[sub ?? 'III'] ?? ''
})

onMounted(() => {
  store.fetchArenaData()
})
</script>

<style scoped>
.arena-panel {
  display: flex;
  flex-direction: column;
  height: 100%;
  padding: 12px;
  gap: 14px;
  overflow-y: auto;
}

/* ── 加载态 ── */
.arena-panel__loading {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  flex: 1;
  gap: 8px;
  color: var(--text-muted);
  font-size: var(--font-size-caption);
}

.loading-spinner {
  width: 24px;
  height: 24px;
  border: 2px solid rgba(142, 142, 147, 0.2);
  border-top-color: var(--accent-blue);
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

/* ── 赛季标题 ── */
.arena-panel__season {
  text-align: center;
}

.arena-panel__season-label {
  font-size: 11px;
  color: var(--text-muted);
  letter-spacing: 0.1em;
  text-transform: uppercase;
}

.arena-panel__season-name {
  font-size: 20px;
  font-weight: 700;
  color: var(--text-primary);
  margin: 4px 0;
}

.arena-panel__season-countdown {
  font-size: var(--font-size-caption);
  color: var(--accent-blue);
}

/* ── 段位卡片 ── */
.arena-panel__tier-card {
  text-align: center;
  padding: 20px;
  border-radius: 16px;
  border: 1px solid;
}

.tier-card__icon {
  font-size: 36px;
  display: block;
  margin-bottom: 4px;
}

.tier-card__name {
  font-size: 18px;
  font-weight: 700;
  display: block;
}

.tier-card__score {
  font-size: var(--font-size-base);
  color: var(--text-primary);
  margin-top: 8px;
  display: block;
}

.tier-card__progress {
  margin-top: 8px;
  height: 6px;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 3px;
  overflow: hidden;
}

.tier-card__progress-bar {
  height: 100%;
  background: linear-gradient(90deg, #00b894, #00d2ff);
  border-radius: 3px;
  transition: width 0.5s ease;
}

.tier-card__remaining {
  font-size: 11px;
  color: var(--text-muted);
  margin-top: 4px;
  display: block;
}

.tier-card__remaining--max {
  color: var(--accent-gold);
}

/* ── 战绩统计 ── */
.arena-panel__stats {
  display: flex;
  gap: 8px;
}

.stat-item {
  flex: 1;
  text-align: center;
  padding: 8px;
  background: var(--bg-panel-light);
  border-radius: 10px;
}

.stat-item__value {
  font-size: 18px;
  font-weight: 700;
  display: block;
}

.stat-item__value--win { color: var(--accent-green); }
.stat-item__value--lose { color: var(--accent-red); }
.stat-item__value--rate { color: var(--accent-blue); }

.stat-item__label {
  font-size: 11px;
  color: var(--text-muted);
}

/* ── 段位列表 ── */
.arena-panel__tier-list {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.tier-row {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 6px 8px;
  border-radius: 8px;
  border: 1px solid transparent;
}

.tier-row__icon {
  font-size: 14px;
}

.tier-row__name {
  font-size: var(--font-size-caption);
  font-weight: 600;
}

.tier-row__range {
  margin-left: auto;
  font-size: 11px;
  color: var(--text-muted);
}

.tier-row__badge {
  font-size: 10px;
  background: var(--accent-blue);
  color: white;
  padding: 1px 6px;
  border-radius: 4px;
}

/* ── 空状态 ── */
.arena-panel__empty {
  display: flex;
  align-items: center;
  justify-content: center;
  flex: 1;
  color: var(--text-muted);
  font-size: var(--font-size-small);
}
</style>
```

- [ ] **Step 2: Commit**

```bash
git add src/components/arena/ArenaPanel.vue
git commit -m "feat: add ArenaPanel component with tier display"
```

---

### Task 6: 修改 HomeView 集成竞技场

**Files:**
- Modify: `src/views/HomeView.vue`

- [ ] **Step 1: 添加 import**

在 HomeView.vue 的 `<script setup>` 中添加导入（在 LeaderboardPanel 导入之后）：

```typescript
import ArenaPanel from '../components/arena/ArenaPanel.vue'
```

注意：Swords 图标已经从 lucide-vue-next 导入。

- [ ] **Step 2: 扩展 centerView 类型**

将 centerView ref 的类型扩展为包含 `'arena'`：

找到类似 `const centerView = ref<'home' | 'map' | ...>` 的行，在类型联合末尾（`'leaderboard'` 后面）添加 `| 'arena'`。

- [ ] **Step 3: 添加竞技场视图区域**

在模板中 leaderboard 视图之后添加：

```html
<div v-else-if="centerView === 'arena'" class="game-panel game-main">
  <ArenaPanel />
</div>
```

- [ ] **Step 4: 添加底部导航 Tab**

在底部导航栏中"排行"Tab 之后、"商店"Tab 之前添加"竞技"Tab：

```html
<div class="game-panel game-bottom-nav__item" :class="{ 'game-bottom-nav__item--active': centerView === 'arena' }" @click="centerView = 'arena'">
  <Swords :size="16" />
  <span>竞技</span>
</div>
```

- [ ] **Step 5: Commit**

```bash
git add src/views/HomeView.vue
git commit -m "feat: integrate arena tab into HomeView navigation"
```

---

### Task 7: 创建后端对接文档 + 更新开发方案 + 构建验证

**Files:**
- Create: `竞技场入口与赛季展示-后端对接文档.md`
- Modify: `开发方案.md`

- [ ] **Step 1: 创建后端对接文档**

创建 `竞技场入口与赛季展示-后端对接文档.md`：

```markdown
# 竞技场入口与赛季展示 - 后端对接文档

## 概述

竞技场系统展示当前赛季信息、玩家段位和战绩统计。

---

## 1. API 接口

### 1.1 获取当前赛季

GET /arena/season

响应：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "seasonId": "season-3",
    "seasonName": "龙焰纪元",
    "seasonNumber": 3,
    "startDate": "2026-04-15T00:00:00Z",
    "endDate": "2026-06-15T00:00:00Z",
    "isActive": true
  }
}
```

### 1.2 获取玩家竞技数据

GET /arena/me

响应：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "tier": "platinum",
    "subTier": "II",
    "score": 2050,
    "wins": 42,
    "losses": 28,
    "winRate": 60,
    "seasonId": "season-3"
  }
}
```

---

## 2. 段位体系

| 段位 | 积分范围 | 小级 |
|:---|:---|:---|
| 青铜 | 0-599 | I/II/III |
| 白银 | 600-1199 | I/II/III |
| 黄金 | 1200-1799 | I/II/III |
| 铂金 | 1800-2399 | I/II/III |
| 钻石 | 2400-2799 | I/II/III |
| 王者 | 2800+ | 无 |

---

## 3. 前端 Mock 行为

| 功能 | Mock 实现 |
|:---|:---|:---|
| 赛季数据 | 固定返回 S3 · 龙焰纪元 |
| 玩家数据 | 固定返回铂金 II / 2050 分 |
| 段位解析 | 前端 resolveTier() 本地计算 |

---

## 4. 类型定义

参见 src/types/arena.ts
```

- [ ] **Step 2: 更新开发方案第 533 行**

将 `| 竞技场入口与赛季展示 | P0 | PVP 入口和赛季信息 |` 改为 `| 竞技场入口与赛季展示 | P0 | ✅ 已完成 | 底部导航竞技Tab、6级段位系统、赛季倒计时、战绩统计、后端对接文档 |`

- [ ] **Step 3: 运行构建验证**

```bash
cd "/Users/hyacinth/Desktop/project sword" && npm run build
```

Expected: 构建成功，无 TypeScript 错误

- [ ] **Step 4: Commit**

```bash
git add 竞技场入口与赛季展示-后端对接文档.md 开发方案.md
git commit -m "docs: add arena backend doc and mark feature as completed"
```
